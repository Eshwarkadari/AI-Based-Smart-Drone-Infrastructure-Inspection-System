services:
  - type: web
    name: aero-vaani-inspection
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn run:app
    plan: free
    envVars:
      - key: SIMULATION_MODE
        value: "True"
      - key: FLASK_SECRET_KEY
        generateValue: true
